snippet		sh
abbr		#!/bin/sh
options		head
	#!/bin/sh
	${0}

snippet		getopts
abbr		while getopts
options		head
	while getopts '${1:h}' flag; do
		case "$flag" in
			${2:#flag})
				${3:init}
				;;
			h)
				echo "Usage: \$0 [-h] ${0}"
				exit 0
		esac

	done
	shift $(( $OPTIND - 1 ))
